export interface Member {
  id: string;
  name: string;
  role: string;
  avatar: string;
  email: string;
}
export interface Participant {
  id: string;
  role: string;
  image: string;
  email: string;
  name: string;
}
